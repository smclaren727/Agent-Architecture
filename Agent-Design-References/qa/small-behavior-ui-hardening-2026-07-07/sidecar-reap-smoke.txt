Packaged-app quit smoke — COMPLETE 2026-07-07 ~17:35 EDT (all legs PASS)

Bundle freshness (stale-binary trap check): PASS
  Agent Vault.app Contents/MacOS/agent-vault-server  sha256 c50975ca3cf6… == staged target/release binary
  Agent Overlay Console.app agent-overlay-server     sha256 e32dfeb5e0db… == staged target/release binary
  (both bundles rebuilt from the fixed commits Vault 0e0655a / Overlay 69131a8)

Fixed-release-binary reap proof (the exact pre-fix failure mode): PASS
  Python parent spawns the bundled binary with the parent-PID env and PIPED
  stderr, then dies → both fixed binaries exit on their own (pre-fix: both
  survived indefinitely; see sidecar-reap-repro.md).

Real .app smoke, Vault (ports 4173/4183): PASS
  Launch: sidecar (91641) child of app (91632); /api/health ok:true with the
  matching per-launch instanceToken in <1s; clean boot in the app log.
  Graceful quit (osascript quit ≙ Cmd+Q): app + sidecar gone ≤3s, both ports
  released.
  Abnormal death (kill -9 the shell, app 92232 / server 92238): sidecar
  self-reaped ≤3s via the fixed watchdog, ports released.

Real .app smoke, Overlay (port 4180): PASS
  Launch: sidecar (92271) child of app (92266); /api/health ok:true.
  Graceful quit: app + sidecar gone ≤3s, port released.
  Abnormal death (kill -9 the shell, app 92299 / server 92304): sidecar
  self-reaped ≤3s, port released.

Note on the one red Vault launch smoke observed between the fix landing and
this run: with the ports free the packaged app boots green in ~1s, so the red
run matches the known port-collision trap — a pre-fix orphan (or a prior test
instance) still on 4173 answers the health poll with the WRONG instanceToken,
the shell's 10s gate correctly refuses the stale server and kills its own
just-spawned sidecar (which could never bind). "Direct sidecar works" fits:
a direct run used a free port. The per-launch token gate behaved as designed;
the fixed watchdog now prevents the orphan that set the trap.
